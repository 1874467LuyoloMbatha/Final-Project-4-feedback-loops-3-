using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//Determines the turn the game manager is in. 
public enum currentTurnScript 
{
    // Start is called before the first frame update
   PLAYERTURN,
   OPPTURN
}
